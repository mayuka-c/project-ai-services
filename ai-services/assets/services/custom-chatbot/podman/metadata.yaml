name: custom-chatbot
version: "1.0.0"
podTemplateExecutions:
  - [custom-chatbot.yaml.tmpl]

# Resource requirements
resources:
  cpu: 1                # 500m rounded up
  memory: 536870912     # 512Mi in bytes
  storage: 0            # No persistent storage required
